# MongoDB connection string (required for reserve-a-seat)
# Same cluster as in Compass. Replace <db_password> with your actual database password.
# Esummit database so reservations are stored in Esummit.reservations
MONGODB_URI=mongodb+srv://rvsaxena1821_db_user:<db_password>@cluster0.zpokbfi.mongodb.net/Esummit?retryWrites=true&w=majority
